export interface User {
    uid: string;
    email: string;
    firstName?: string;
    lastName?: string;
    displayName: string;
    photoURL?: string;
    stripeCustomerId?: string;
    // cart?: 
    // orders:
}